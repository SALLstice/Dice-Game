using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class debugtonScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

    }

    public void onMouseDown()
    {
        //Debug.Log("debugton");
        //var debug1 = GameObject.Find("Dice Sides").GetComponent<diceSidesScript>().randFace();

        //Debug.Log("random face: " + debug1.randFace());
    }
}
